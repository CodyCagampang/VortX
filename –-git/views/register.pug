extends layout

block content
  .row.banner
    .col-12
      h1= pageHeader.title
      small &nbsp;#{pageHeader.strapline}
  
  .row
    .col-12.col-md-6.offset-md-3
      .card
        .card-body
          p.text-center Registration functionality coming soon!
          .text-center.mt-3
            a.btn.btn-primary(href="/") Return to Home
            
          .mt-3.text-center
            p Already have an account? 
              a(href="/login") Login here